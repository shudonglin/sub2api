version: 1
exceptions:
  - package: lodash
    advisory: "GHSA-r5fr-rjxr-66jc"
    severity: high
    reason: "lodash _.template is not used with untrusted input in this codebase. lodash is a transitive dep via @lobehub/ui (ahooks) and eslint-plugin-vue. No fix is available upstream — lodash maintainers have not patched this."
    mitigation: "Audited all _.template usage — none accept user-controlled templates. Will migrate ahooks consumers if a safer alternative ships."
    expires_on: "2026-07-02"
    owner: "security@your-domain"
  - package: lodash-es
    advisory: "GHSA-r5fr-rjxr-66jc"
    severity: high
    reason: "lodash-es _.template is not used with untrusted input in this codebase. Transitive dep via mermaid (chevrotain → lodash-es). Same upstream unpatched issue as lodash."
    mitigation: "No user-controlled template strings used. Will update when chevrotain / mermaid ships a fix."
    expires_on: "2026-07-02"
    owner: "security@your-domain"
